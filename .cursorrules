# AIICG壁纸站 Cursor AI 规则

## 🚫 强制规则 (违反者代码将被拒绝)

### 零容忍硬编码中文
- 绝对禁止在代码中直接写中文字符串
- 任何用户可见的文本都必须使用翻译函数 t()
- 错误示例: `<button>提交</button>` 或 `const message = "操作成功"`
- 正确示例: `<button>{t('common.submit')}</button>` 或 `const message = t('messages.success')`

### 必须使用国际化
- 所有组件必须导入并使用 useLanguage hook
- 每个新功能必须同时提供中英文翻译
- 在 LanguageContext 中添加完整的翻译键

## 🌍 国际化规范

### Context 使用模式
```typescript
import { useLanguage } from '@/contexts/LanguageContext';

const { t, locale, setLocale } = useLanguage();
```

### 翻译键命名规范
- 页面标题: `pages.{页面}.title`
- 导航菜单: `nav.{菜单项}`
- 按钮文字: `buttons.{功能}`
- 状态信息: `status.{状态}`
- 表单标签: `forms.{字段}`
- 错误信息: `errors.{类型}`

### 翻译覆盖要求
必须翻译的内容:
- 所有按钮和链接文字
- 页面标题和描述
- 表单标签和占位符
- 状态和错误信息
- 工具提示和帮助文本

无需翻译的内容:
- 代码注释
- 控制台日志
- API 接口路径
- 文件名和路径

## 🎨 UI/UX 规范

### 设计原则
- 参考即梦AI风格: 16px宽侧边栏，图标+文字垂直布局
- 使用 backdrop-blur-md 毛玻璃效果
- 统一使用 Lucide-react 图标库
- 遵循设计系统: src/styles/design-system.ts

### 响应式设计
```typescript
// 断点使用
sm: '640px'   // 手机横屏
md: '768px'   // 平板
lg: '1024px'  // 桌面
xl: '1280px'  // 大屏

// 侧边栏适配
className="w-16 lg:w-20"
```

### 主题系统
```typescript
// 深浅色模式适配
className="bg-white dark:bg-neutral-900 text-neutral-900 dark:text-white"
```

## 📱 组件开发规范

### 必需导入和结构
```typescript
'use client';

import { useState } from 'react';
import { useLanguage } from '@/contexts/LanguageContext';

export default function Component() {
  const { t } = useLanguage(); // 必需!
  
  return (
    <div>
      <h1>{t('component.title')}</h1>
    </div>
  );
}
```

### 组件要求
- 必须导入并使用 useLanguage
- 严格 TypeScript 类型定义
- 实现键盘导航和 ARIA 标签
- 移动端优先的响应式设计
- 合理使用 React.memo 和懒加载

## 🔍 代码质量标准

### 提交前检查清单
- [ ] 无硬编码中文 (使用 grep -r "[\u4e00-\u9fff]" src/ 检查)
- [ ] 完整翻译覆盖 (中英文都已添加)
- [ ] TypeScript 类型检查通过
- [ ] ESLint 检查无错误
- [ ] 响应式测试通过
- [ ] 深浅色模式都正常

### 常见错误避免
| 错误类型 | 错误示例 | 正确示例 |
|----------|----------|----------|
| 硬编码中文 | `"下载"` | `t('buttons.download')` |
| 缺失翻译键 | `t('nonexistent.key')` | 先在 LanguageContext 中定义 |
| 类型错误 | `setLocale('invalid')` | `setLocale('zh-CN' \| 'en')` |

## 📋 提交规范

### Git 提交消息
```
feat: 添加国际化支持
fix: 修复侧边栏翻译缺失
refactor: 重构组件使用翻译函数
```

### Pull Request 要求
- 标题必须说明是否涉及国际化
- 描述中列出新增的翻译键
- 确保 CI/CD 检查全部通过
- 自测中英文切换功能

## 🚨 错误处理

### 违规处理流程
1. **严重违规** (硬编码中文): 立即拒绝，要求重新开发
2. **中等违规** (翻译不完整): 补充翻译后重新提交
3. **轻微违规** (代码风格): ESLint 自动修复

### 检查命令
```bash
# 检查硬编码中文
grep -r "[\u4e00-\u9fff]" src/ --include="*.tsx" --include="*.ts"

# TypeScript 检查
npm run type-check

# 代码质量检查
npm run lint

# 构建测试
npm run build
```

## 📚 最佳实践参考

### 示例文件
- 参考 `src/components/SideNav.tsx` 的国际化实现
- 查看 `src/contexts/LanguageContext.tsx` 的翻译结构
- 学习 `issues/` 目录下的任务记录

### 扩展阅读
- Cursor Rules 最佳实践
- Next.js 国际化指南
- React 性能优化建议

---

**记住**: 国际化不是可选项，是必需品！每一行用户可见的代码都要考虑全球用户体验。

**目标**: 让 AIICG壁纸站 成为真正的国际化产品，服务全球用户！ 